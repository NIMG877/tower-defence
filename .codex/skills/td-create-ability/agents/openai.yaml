interface:
  display_name: "TD Create Ability"
  short_description: "Compose AbilityConfig rules and ordered steps"
  default_prompt: "Use $td-create-ability to compose an AbilityConfig asset with rules, ordered steps, and verified operations."
